/**
 * PlanView - stub to unblock build
 * Full implementation pending V66+
 */
import React from 'react';

interface PlanViewProps {
  onExecute?: (plan: unknown) => void;
  onCancel?: () => void;
}

export const PlanView: React.FC<PlanViewProps> = () => null;
